## Summary

<!-- What changed and why? -->

## Test plan

- [ ] `uv run ruff format --check .`
- [ ] `uv run ruff check .`
- [ ] `uv run pytest -q`
- [ ] Manual CLI check if user-facing (`scan` / `tag` / `rename` / `list` / `search`)

## Checklist

- [ ] No secrets or local paths committed
- [ ] Docs / CHANGELOG updated when behavior changes
- [ ] New public APIs have Google-style docstrings
